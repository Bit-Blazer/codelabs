{
  "name": "codelabs-uploader-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for handling codelab uploads and GitHub PR creation",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
